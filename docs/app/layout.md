下面我按**实际 Flutter 项目中最常见、最成熟、最不容易走弯路**的方式，系统性地给你总结 **Flutter 项目文件布局（目录结构）**。内容会从**官方默认 → 中小型项目 → 中大型项目**逐级展开，并明确**每一层该放什么、不该放什么**。

---

## 一、Flutter 官方默认结构（了解即可）

```txt
my_app/
 ├── android/
 ├── ios/
 ├── lib/
 │    └── main.dart
 ├── test/
 ├── pubspec.yaml
```

问题：

* 所有代码堆在 `main.dart`
* 不适合真实项目

👉 **只能用于 Demo，不适合长期维护**

---

## 二、中小型项目（最常用 ⭐⭐⭐⭐⭐）

> **这是 80% Flutter 项目的首选结构**

### 1️⃣ 推荐目录结构

```txt
lib/
 ├── main.dart              // 入口
 ├── app.dart               // App 根组件
 ├── routes/
 │    └── app_router.dart   // 路由配置（go_router）
 │
 ├── pages/                 // 页面级
 │    ├── home/
 │    │    ├── home_page.dart
 │    │    ├── home_view_model.dart
 │    │    └── home_state.dart
 │    ├── login/
 │    └── profile/
 │
 ├── models/                // 数据模型
 │    └── user.dart
 │
 ├── services/              // 业务服务
 │    ├── api_service.dart
 │    └── auth_service.dart
 │
 ├── widgets/               // 通用组件
 │    └── app_button.dart
 │
 ├── utils/                 // 工具类
 │    ├── constants.dart
 │    └── helpers.dart
 │
 └── theme/
      └── app_theme.dart
```

---

### 2️⃣ 各目录职责说明（非常重要）

#### `main.dart`

* 程序入口
* 初始化依赖（如 Provider / Riverpod）
* 不写 UI 逻辑

```dart
void main() {
  runApp(const MyApp());
}
```

---

#### `app.dart`

* `MaterialApp`
* 主题、路由注入

```dart
MaterialApp.router(
  routerConfig: router,
);
```

---

#### `routes/`

* **集中管理路由**
* 不要分散在页面里

```dart
GoRoute(
  path: '/login',
  builder: (_, __) => LoginPage(),
);
```

---

#### `pages/`

> **一个页面一个文件夹**

```txt
home/
 ├── home_page.dart        // UI
 ├── home_view_model.dart  // 业务逻辑
 └── home_state.dart       // 状态数据
```

**原则：**

* UI 不直接写网络请求
* 页面只“展示状态”

---

#### `models/`

* 纯数据结构
* `fromJson / toJson`

```dart
class User {
  final String name;
  User.fromJson(Map<String, dynamic> json);
}
```

---

#### `services/`

* 网络
* 登录
* 数据处理

```dart
class ApiService {
  Future get(String path) {}
}
```

---

#### `widgets/`

* 可复用组件
* 不依赖业务

```dart
AppButton(text: '提交');
```

---

#### `utils/`

* 常量
* 工具函数
* 日期格式化

---

#### `theme/`

* 颜色
* 字体
* 样式规范

---

## 三、中大型项目（团队 / 商业项目）

> **模块化结构（Feature First）**

### 1️⃣ 结构示例

```txt
lib/
 ├── core/
 │    ├── network/
 │    ├── router/
 │    ├── theme/
 │    └── utils/
 │
 ├── features/
 │    ├── auth/
 │    │    ├── auth_page.dart
 │    │    ├── auth_vm.dart
 │    │    ├── auth_service.dart
 │    │    └── auth_model.dart
 │    │
 │    ├── home/
 │    └── profile/
 │
 ├── shared/
 │    ├── widgets/
 │    └── models/
 │
 └── main.dart
```

**特点：**

* 每个功能是一个独立模块
* 适合多人协作
* 可拆包 / 插件化

---

## 四、常见错误布局（一定要避开）

❌ **所有页面平铺**

```txt
lib/
 ├── home.dart
 ├── login.dart
 ├── profile.dart
```

❌ **UI + 网络请求混在一起**

```dart
Widget build() {
  fetchData(); // 错误
}
```

❌ **路由写在页面里**

```dart
Navigator.push(...); // 分散管理
```

---

## 五、和你前端背景的对照（React / Vue）

| Flutter    | React / Vue        |
| ---------- | ------------------ |
| pages      | views / pages      |
| widgets    | components         |
| view_model | hooks / store      |
| services   | api                |
| models     | types / interfaces |

---

## 六、我给你的实用建议（结合你学习阶段）

* **现在就用「中小型项目结构」**
* 一开始就拆目录，不要等代码多了再拆
* 用 `go_router + Provider/Riverpod`

---

## 七、下一步我可以继续帮你做什么

你可以直接选一个：

1. **给你一个标准 Flutter 项目模板**
2. **手把手搭一个目录 + 路由 + 首页**
3. **对比 Provider / Riverpod，帮你选**
4. **用一个完整 Demo 把这个目录跑起来**

你只要说一句：
👉 *“按中小型结构，带我搭一个项目”*
