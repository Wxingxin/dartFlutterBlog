# 运算符

## 🧠 总结表格

| 分类                            | 运算符               | 功能                         |
| ------------------------------- | -------------------- | ---------------------------- |
| 1.算术运算符                    | +, -, \*, /, ~/ , %  | 基本数学计算                 |
| 2.关系（比较）运算符            | ==, !=, >, <, >=, <= | 比较值关系                   |
| 3.类型判断运算符                | !, &&,               |                              |
| 4. 赋值运算符                   | ?., ??, ??=          | 防止 null 报错，设置默认值   |
| 5.逻辑运算符                    | ..                   | 多次访问同一对象的属性或方法 |
| 6.位运算符                      | is, is!              | 判断对象类型                 |
| 条件运算符（三目／？？）        | a                    | a                            |
| 8.级联运算符(../？..) Dart 特有 | a                    | a                            |

## 一、算术运算符

| 运算符 | 含义         | 示例          |
| ------ | ------------ | ------------- |
| `+`    | 加           | `a + b`       |
| `-`    | 减           | `a - b`       |
| `*`    | 乘           | `a * b`       |
| `/`    | 除（double） | `5 / 2 → 2.5` |
| `~/`   | 整除         | `5 ~/ 2 → 2`  |
| `%`    | 取余         | `5 % 2 → 1`   |

### 🌰 经典示例：

```dart
void main() {
  int a = 10;
  int b = 3;

  print(a + b);   // 加法：13
  print(a - b);   // 减法：7
  print(a * b);   // 乘法：30
  print(a / b);   // 除法（结果是double）：3.3333333333333335
  print(a ~/ b);  // 整除：3
  print(a % b);   // 取模（余数）：1
}
```

💡 **技巧：**

- `/` 返回 `double` 类型；
- `~/` 才是“真正意义的整除”。

## 二、关系和相等运算符

| 运算符    | 含义                |
| --------- | ------------------- |
| `==`      | 相等                |
| `!=`      | 不等                |
| `>` `<`   | 大 / 小             |
| `>=` `<=` | 大于等于 / 小于等于 |

### 🌰 示例：

```dart
void main() {
  int a = 5;
  int b = 10;

  print(a == b);  // false
  print(a != b);  // true
  print(a > b);   // false
  print(a < b);   // true
  print(a >= 5);  // true
  print(b <= 10); // true
}
```

== 可被重载

Object 默认是值比较，不是地址比较（和 Java 不一样）

## 三、类型判断运算符

| 运算符 | 作用             |
| ------ | ---------------- |
| `is`   | 是否是某类型     |
| `is!`  | 是否不是某类型   |
| `as`   | 类型转换（强转） |

### 🌰 示例：

```dart
Object x = 'hello';

if (x is String) {
  print(x.length); // 自动类型提升
}

var y = x as String;

```

⚠️ as 转错类型 👉 运行时异常

## 四、赋值运算符

## 1️⃣ 基本赋值

```dart
a = 10;
```

---

## 2️⃣ 复合赋值

| 运算符 | 示例      |
| ------ | --------- |
| `+=`   | `a += 1`  |
| `-=`   | `a -= 1`  |
| `*=`   | `a *= 2`  |
| `/=`   | `a /= 2`  |
| `~/=`  | `a ~/= 2` |
| `%=`   | `a %= 2`  |

---

## 3️⃣ 空值赋值（⭐ Dart 重点）

### `??=`

```dart
int? a;
a ??= 10;
print(a); // 10
```

👉 **只有在左边为 null 时才赋值**

## 五、逻辑运算符

| 运算符 | 含义 |     |     |
| ------ | ---- | --- | --- |
| `&&`   | 与   |     |     |
| `      |      | `   | 或  |
| `!`    | 非   |     |     |

```dart
if (age > 18 && isStudent) {}
```

⚠️ **短路规则**

- `&&` 左边 false → 右边不执行
- `||` 左边 true → 右边不执行

## 六、位运算符

| 运算符 | 含义     |        |
| ------ | -------- | ------ |
| `&`    | 按位与   |        |
| `      | `        | 按位或 |
| `^`    | 按位异或 |        |
| `~`    | 按位取反 |        |
| `<<`   | 左移     |        |
| `>>`   | 右移     |        |

```dart
print(5 & 3); // 1
print(5 | 3); // 7
```

👉 常见于：

- 权限位
- 二进制状态标记
- 算法题

## 七、条件运算符(三目／ ？？)

### 1️⃣ 三目运算符 `?:`

```dart
var result = a > b ? a : b;
```

### 2️⃣ 空值判断运算符 `??`

```dart
var name = input ?? 'default';
```

👉 **input 为 null 才使用默认值**

### 3️⃣ 空安全访问 `?.`

```dart
user?.name;
```

- user 为 null 👉 整个表达式返回 null
- 不抛异常

## 八、级联运算符（./？..）★Dart 特有

### 1️⃣ `..`（级联调用）

```dart
var buffer = StringBuffer()
  ..write('Hello')
  ..write(' ')
  ..write('World');
```

👉 等价于：

```dart
var buffer = StringBuffer();
buffer.write('Hello');
buffer.write(' ');
buffer.write('World');
```

### 2️⃣ `?..`（空安全级联）

```dart
obj
  ?..method1()
  ..method2(); // ⚠️ 只有前一个为 null 才跳过
```

## 其他常见运算符

| 运算符 | 说明     |
| ------ | -------- |
| `[]`   | 下标访问 |
| `[]=`  | 下标赋值 |
| `.`    | 成员访问 |
| `!`    | 非空断言 |
| `=>`   | 箭头函数 |

```dart
list[0] = 10;
print(obj!.value);
```

⚠️ `!` 用不好 = **空指针异常**

## 真实开发高频组合（非常实用）

### ✅ 1. 默认值处理

```dart
String name = userName ?? '游客';
```

---

### ✅ 2. 空安全链式调用

```dart
user?.profile?.avatarUrl;
```

---

### ✅ 3. 状态切换

```dart
isOpen = !isOpen;
```

---

### ✅ 4. Flutter 中条件渲染

```dart
child: isLogin ? HomePage() : LoginPage()
```
