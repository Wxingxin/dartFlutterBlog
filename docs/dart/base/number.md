下面给你一份**📘 Dart 语言 Number（数字类型）知识点大全**，按**基础 → API → 运算 → 转换 → Flutter / 面试坑**系统整理，和你前面的 **String / List / Map / Set** 完全同一体系。

---

# 一、Dart 的数字类型总览（必背）

Dart 中的数字类型主要有 **3 个**：

```dart
num    // 抽象父类
int    // 整数
double // 浮点数
```

👉 继承关系：

```
Object
 └── num
     ├── int
     └── double
```

---

# 二、int（整数）

## 1️⃣ 定义

```dart
int a = 10;
var b = 20; // 自动推断 int
```

---

## 2️⃣ 特点（重要）

* **没有 long / short / byte**
* 整数 **自动扩容（大整数）**
* 不会溢出（不像 Java）

```dart
int big = 999999999999999999999;
```

---

## 3️⃣ 常用属性 / 方法

```dart
a.isEven
a.isOdd
a.abs()
a.sign        // -1 / 0 / 1
```

---

## 4️⃣ 位运算（了解）

```dart
a << 1
a >> 1
a & b
a | b
a ^ b
```

---

# 三、double（浮点数）

## 1️⃣ 定义

```dart
double d = 3.14;
var x = 2.0;
```

---

## 2️⃣ 特点（⚠️ 面试点）

* IEEE 754 双精度
* **存在精度误差**

```dart
print(0.1 + 0.2); // 0.30000000000000004
```

---

## 3️⃣ 常用方法

```dart
d.floor();   // 向下取整
d.ceil();    // 向上取整
d.round();   // 四舍五入
d.truncate();// 直接截断
```

---

## 4️⃣ 保留小数位

```dart
d.toStringAsFixed(2); // "3.14"
```

---

# 四、num（int 和 double 的父类）

## 1️⃣ 定义

```dart
num x = 10;
x = 3.14;
```

👉 当你不确定是 int 还是 double 时用 `num`

---

## 2️⃣ 常用方法（通用）

```dart
x.abs();
x.ceil();
x.floor();
x.round();
x.toInt();
x.toDouble();
```

---

# 五、数字运算符（必会）

## 1️⃣ 算术运算

```dart
+  -  *  /  %
```

⚠️ 注意：

```dart
5 / 2;  // 2.5  → double
5 ~/ 2; // 2    → int（整除）
```

---

## 2️⃣ 自增 / 自减

```dart
a++;
++a;
a--;
```

---

## 3️⃣ 复合赋值

```dart
a += 1;
a *= 2;
```

---

# 六、比较运算（基础）

```dart
a == b
a != b
a > b
a < b
a >= b
a <= b
```

👉 数字比较是 **值比较**

---

# 七、类型判断 & 转换（高频）

## 1️⃣ 类型判断

```dart
x is int
x is double
x is num
```

---

## 2️⃣ 数字 → 数字

```dart
int a = 3;
double b = a.toDouble();

double c = 3.7;
int d = c.toInt(); // 3
```

---

## 3️⃣ String ↔ Number（必考）

### String → Number

```dart
int.parse("123");
double.parse("3.14");
```

⚠️ 安全写法：

```dart
int.tryParse("abc"); // null
```

---

### Number → String

```dart
123.toString();
3.14159.toStringAsFixed(2); // "3.14"
```

---

# 八、null safety 与 Number（必考）

```dart
int? a;
double? b;
num? c;
```

安全使用：

```dart
if (a != null) {
  print(a + 1);
}
```

或：

```dart
print((a ?? 0) + 1);
```

---

# 九、常用数学函数（dart:math）

```dart
import 'dart:math';

max(3, 5);
min(3, 5);
pow(2, 3);      // 8
sqrt(16);       // 4
Random().nextInt(10);
```

---

# 十、浮点数比较（⚠️ 面试高频）

❌ 错误方式：

```dart
if (a == b) {}
```

✅ 正确方式：

```dart
const eps = 1e-10;
if ((a - b).abs() < eps) {}
```

---

# 十一、Flutter 中 Number 的典型场景

## 1️⃣ UI 显示格式化

```dart
Text("${price.toStringAsFixed(2)} 元");
```

---

## 2️⃣ 表单输入转数字

```dart
int count = int.tryParse(controller.text) ?? 0;
```

---

## 3️⃣ 进度 / 比例计算

```dart
double percent = current / total;
```

---

# 十二、Number 常见错误总结（面试必考）

❌ 认为 `/` 是整除
❌ 浮点数直接 `==` 比较
❌ parse 不处理异常
❌ 混淆 `~/` 和 `/`
❌ 忽略 null safety

---

# 十三、一句话记忆版

> **Dart 的 Number = int 自动扩容 + double 有精度问题 + num 统一操作**

---

## 下一步我可以继续帮你👇

* ✅ **Dart bool / enum / Object**
* ✅ **Dart 运算符大全**
* ✅ **Dart 类型系统（dynamic / Object / var）**
* ✅ **Dart 基础面试题合集**

你直接说下一步学哪个 👍
