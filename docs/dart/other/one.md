这是 **Dart 中 `var / final / const / late`** 在**同一段代码里的真实差异**。我按**是否可变、初始化时机、运行期行为**给你一次讲透。

---

## 一眼看懂对比表（先看这个）

| 写法                | 是否必须立即初始化 | 是否可重新赋值 | 是否可修改内容 | 编译期/运行期 | 典型用途       |
| ----------------- | --------- | ------- | ------- | ------- | ---------- |
| `var list`        | ✅         | ✅       | ✅       | 运行期     | 普通局部变量     |
| `final listFinal` | ✅         | ❌       | ✅       | 运行期     | 不希望被重新指向   |
| `const listConst` | ✅         | ❌       | ❌       | **编译期** | 常量 / 不可变数据 |
| `late listLate`   | ❌         | ✅       | ✅       | 运行期     | 延迟初始化      |

---

## 1️⃣ `var list = [1, 2, 3];`

### 本质

```dart
var list = [1, 2, 3];
```

* 编译期**类型推断** → `List<int>`
* 普通变量

### 行为

```dart
list.add(4);      // ✅
list = [4, 5];    // ✅
```

### 适合

* 方法内部临时变量
* 会被多次赋值

---

## 2️⃣ `final listFinal = [1, 2, 3];`

### 本质

```dart
final listFinal = [1, 2, 3];
```

* 类型推断：`List<int>`
* **引用不可变**（只能赋值一次）

### 行为

```dart
listFinal.add(4);     // ✅
listFinal = [4, 5];   // ❌ 编译错误
```

> 📌 `final` 只锁“指向”，不锁“内容”

### 适合

* Widget 中的属性
* 逻辑上“不会换对象”的变量
* Flutter 推荐大量使用

---

## 3️⃣ `const listConst = [1, 2, 3];`

### 本质（⚠️最重要）

```dart
const listConst = [1, 2, 3];
```

* **编译期常量**
* 类型：`List<int>`
* **深度不可变**

### 行为

```dart
listConst.add(4);   // ❌
listConst[0] = 10;  // ❌
```

### 限制

```dart
const a = DateTime.now(); // ❌（不是编译期常量）
```

### 适合

* 配置
* 枚举值
* 静态常量
* Flutter 的 `const Widget`

---

## 4️⃣ `late listLate = [1, 2, 3];`

### 本质

```dart
late listLate = [1, 2, 3];
```

* **延迟初始化**
* 类型推断：`List<int>`
* 绕过 Dart 的空安全检查

> ⚠️ 注意：你这个写法其实 **没有真正“延迟”**
>
> ```dart
> late listLate = [1, 2, 3]; // 仍然是立刻初始化
> ```

### 真正的 `late` 用法

```dart
late List<int> listLate;

void init() {
  listLate = [1, 2, 3];
}
```

### 运行期风险

```dart
print(listLate.length); // ❌ 未初始化会抛异常
```

### 适合

* `initState` 才能拿到的值
* 依赖 `BuildContext`
* 依赖异步结果（但非 async）

---

## 🔥 四者核心差异总结（记这 4 句话）

1. **`var`**：普通变量，能改能换
2. **`final`**：只能赋值一次，但内容可变
3. **`const`**：编译期常量，内容和引用都不可变
4. **`late`**：告诉编译器“我保证之后会赋值”

---

## Flutter / 实战建议（很重要）

### ✅ 推荐写法

```dart
final items = <int>[1, 2, 3];
```

### 🚫 不推荐滥用

```dart
late var list = [1, 2, 3]; // ❌ 毫无意义
```

### Widget 中典型组合

```dart
class MyWidget extends StatelessWidget {
  final List<int> items;

  const MyWidget({super.key, required this.items});
}
```

---

## 如果你愿意下一步

我可以帮你总结一套 **「Flutter 中什么时候用 var / final / const / late」的肌肉记忆规则表**，让你以后一眼就能选对。
