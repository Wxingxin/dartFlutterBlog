## 一、一句话本质区别（先记这个）

> **`Object` 是所有类的“祖先类型”，`class` 是用来“定义类的语法关键字”。**

下面我分层给你讲，保证你一看就懂，而且不容易混。

| 对比点       | Object                  | class                   |
| ------------ | ----------------------- | ----------------------- |
| 是什么       | 一个 **类 / 类型**      | 一个 **关键字**         |
| 作用         | 所有 Dart 类的 **根类** | 用来 **定义自定义类**   |
| 是否能直接用 | ✅ 可以作为类型         | ❌ 不能作为类型         |
| 是否只有一个 | ✅ 只有一个 Object      | ❌ 可以定义无数个 class |



## 二、`Object` 是什么？

### 1️⃣ `Object` 是 Dart 的**顶级父类**

在 Dart 中：

```dart
class Person {}
```

**等价于：**

```dart
class Person extends Object {}
```

👉 **所有类，最终都继承自 `Object`**



### 2️⃣ `Object` 提供的“通用能力”

所有 Dart 对象，默认都拥有这些方法（来自 `Object`）：

```dart
toString()
== 运算符
hashCode
runtimeType
```

示例：

```dart
class Person {
  String name;
  Person(this.name);
}

void main() {
  var p = Person('Tom');

  print(p.toString());    // Instance of 'Person'
  print(p.hashCode);     // 一个整数
  print(p.runtimeType);  // Person
}
```



### 3️⃣ `Object` 常用作“兜底类型”

当你**什么类型都不想限制**时，用 `Object`：

```dart
Object value;

value = 123;
value = 'hello';
value = true;
```

⚠️ 但注意：
**用 `Object` 后，想用具体方法，必须先类型判断或转换**

```dart
if (value is String) {
  print(value.length);
}
```



## 三、`class` 是什么？

### 1️⃣ `class` 是**定义类的关键字**

`class` 本身**不是类型，也不是对象**，只是语法：

```dart
class Person {
  String name;

  Person(this.name);

  void sayHello() {
    print('Hello, $name');
  }
}
```

你定义的 **`Person`** 才是类型。



### 2️⃣ class 定义了什么？

一个 `class` 可以定义：

- 属性（字段）
- 构造函数
- 方法
- getter / setter
- 静态成员
- 继承关系

```dart
class Car {
  static int count = 0;
  String brand;

  Car(this.brand) {
    count++;
  }

  void drive() {
    print('$brand is driving');
  }
}
```



### 3️⃣ class 用来“建模”，Object 用来“兜底”

👉 **class 是业务建模工具**
👉 **Object 是类型系统的基石**



## 四、`Object` 和 `class` 的真实使用对比

### 例子 1：用 `Object` 作为参数类型

```dart
void printValue(Object value) {
  print(value.toString());
}
```

适合：

- 工具方法
- 日志
- 通用容器



### 例子 2：用 `class` 表达业务含义

```dart
class User {
  String id;
  String name;

  User(this.id, this.name);
}
```

适合：

- 数据模型
- 业务对象
- UI 状态对象



## 五、常见误区（重点）

### ❌ 误区 1：`Object` 和 `class` 都是“类”

✔ **纠正**：

- `Object`：一个**具体存在的类**
- `class`：**定义类的语法工具**



### ❌ 误区 2：`Object` 等于 `dynamic`

**不等！**

```dart
Object a = 'hello';
// a.length ❌ 报错

dynamic b = 'hello';
// b.length ✅
```

👉 `Object` 是**安全但受限**
👉 `dynamic` 是**不安全但灵活**



## 六、什么时候用 Object？什么时候用 class？

### ✅ 用 `Object` 的场景

- 不关心具体类型
- 通用工具方法
- 日志 / 序列化
- 混合类型集合（但更推荐 `Object?`）

```dart
List<Object> list = [1, 'a', true];
```



### ✅ 用 `class` 的场景

- 业务建模
- 数据结构
- Flutter UI 组件
- 状态管理对象

```dart
class AppState {
  bool isLoading;
  AppState(this.isLoading);
}
```



## 七、用一句“工程化”的话总结

> **`class` 是你写代码用的工具，`Object` 是 Dart 语言给你兜底用的根基。**
> 你天天写 `class`，但你写的每一个类，背后都站着 `Object`。



如果你愿意，下一步我可以帮你**对比 `Object / dynamic / Object? / Never` 的使用边界**，这是 Dart 类型系统真正的“分水岭”。


## 

